---
phase: 04-cpp-modernization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/platform/MSWindowsScreenSaver.h
  - src/lib/platform/MSWindowsScreenSaver.cpp
  - src/lib/platform/MSWindowsDesks.h
  - src/lib/platform/MSWindowsDesks.cpp
  - src/lib/platform/OSXScreen.h
  - src/lib/platform/OSXScreen.mm
  - src/lib/platform/PortalInputCapture.h
  - src/lib/platform/PortalInputCapture.cpp
  - src/lib/platform/PortalRemoteDesktop.h
  - src/lib/platform/PortalRemoteDesktop.cpp
autonomous: true
requirements: [MOD-02]

must_haves:
  truths:
    - "MSWindowsScreenSaver 的 m_watch 成员使用 unique_ptr<Thread>，无 raw delete"
    - "MSWindowsDesks::Desk 的 m_thread 成员使用 unique_ptr<Thread>，removeDesks 中先 wait 再 reset"
    - "OSXScreen 的 m_pmWatchThread 成员使用 unique_ptr<Thread>，无 raw delete"
    - "PortalInputCapture/PortalRemoteDesktop 的 m_glibThread 使用 unique_ptr（如果文件仍存在）"
  artifacts:
    - path: "src/lib/platform/MSWindowsScreenSaver.h"
      provides: "unique_ptr<Thread> m_watch 成员"
      contains: "unique_ptr<Thread> m_watch"
    - path: "src/lib/platform/MSWindowsDesks.h"
      provides: "Desk struct 中 unique_ptr<Thread> m_thread 成员"
      contains: "unique_ptr<Thread> m_thread"
    - path: "src/lib/platform/OSXScreen.h"
      provides: "unique_ptr<Thread> m_pmWatchThread 成员"
      contains: "unique_ptr<Thread> m_pmWatchThread"
  key_links:
    - from: "src/lib/platform/MSWindowsDesks.cpp:removeDesks"
      to: "unique_ptr<Thread>::reset()"
      via: "先 wait() 再 reset() 的顺序"
      pattern: "m_thread->wait.*\n.*m_thread.reset()"
---

<objective>
将所有 raw new Thread 调用点替换为 unique_ptr<Thread>（per D-01, D-02），保留自定义 Thread 类（Apple Clang 不支持 std::jthread）。

Purpose: 消除所有 Thread 创建点的手动 new/delete 配对风险，使用代码库已有的 unique_ptr<Thread> 模式（参考 MSWindowsWatchdog）。

Output: 6 个独立调用点的 Thread* 全部替换为 unique_ptr<Thread>。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cpp-modernization/04-CONTEXT.md
@.planning/phases/04-cpp-modernization/04-RESEARCH.md

<interfaces>
<!-- Thread 类构造函数 -->
<!-- 来源: src/lib/mt/Thread.h -->
Thread(IJob* adoptedJob);  // adopt IJob 并在 threadFunc 中 delete
Thread(const Thread&);     // 复制当前线程（用于标识）

<!-- MSWindowsWatchdog 参考实现（代码库中已存在的 unique_ptr<Thread> 模式） -->
<!-- 来源: src/lib/platform/MSWindowsWatchdog.h:133-135 -->
std::unique_ptr<Thread> m_mainThread;
std::unique_ptr<Thread> m_outputThread;
std::unique_ptr<Thread> m_sasThread;

// 构造: m_mainThread = std::make_unique<Thread>(new TMethodJob(...));
// 使用: m_mainThread->wait(kThreadWaitSeconds);
// 析构: unique_ptr 自动调用 Thread::~Thread() -> ARCH->closeThread()

<!-- Lock 类: 接受 Mutex* 参数 -->
<!-- 来源: src/lib/mt/Lock.h -->
explicit Lock(const Mutex *mutex);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all raw new Thread sites with unique_ptr<Thread> across 6 call sites</name>
  <files>
    src/lib/platform/MSWindowsScreenSaver.h,
    src/lib/platform/MSWindowsScreenSaver.cpp,
    src/lib/platform/MSWindowsDesks.h,
    src/lib/platform/MSWindowsDesks.cpp,
    src/lib/platform/OSXScreen.h,
    src/lib/platform/OSXScreen.mm,
    src/lib/platform/PortalInputCapture.h,
    src/lib/platform/PortalInputCapture.cpp,
    src/lib/platform/PortalRemoteDesktop.h,
    src/lib/platform/PortalRemoteDesktop.cpp
  </files>
  <read_first>
    src/lib/platform/MSWindowsScreenSaver.h (m_watch 成员声明)
    src/lib/platform/MSWindowsScreenSaver.cpp (watchDesktop, watchProcess, unwatchProcess 方法)
    src/lib/platform/MSWindowsDesks.h (Desk struct 定义，m_thread 成员)
    src/lib/platform/MSWindowsDesks.cpp (addDesk, removeDesks 方法)
    src/lib/platform/OSXScreen.h (m_pmWatchThread 成员声明)
    src/lib/platform/OSXScreen.mm (m_pmWatchThread 创建和析构)
    src/lib/platform/PortalInputCapture.h (m_glibThread 成员声明)
    src/lib/platform/PortalInputCapture.cpp (m_glibThread 创建)
    src/lib/platform/PortalRemoteDesktop.h (m_glibThread 成员声明)
    src/lib/platform/PortalRemoteDesktop.cpp (m_glibThread 创建)
    src/lib/platform/MSWindowsWatchdog.h:132-135 (unique_ptr<Thread> 参考模式)
  </read_first>

  <action>
  **重要前置步骤：** 对每个文件先执行 `grep -c "new Thread"` 确认 raw new Thread 仍存在。如果 Phase 3 已删除 PortalInputCapture/PortalRemoteDesktop 文件，则跳过这些文件。

  **每个调用点的修改模式：**

  **站点 1: MSWindowsScreenSaver.cpp（行 176, 189）+ .h（行 70）**

  头文件修改:
  ```cpp
  // 替换前 (MSWindowsScreenSaver.h:70):
  Thread *m_watch = nullptr;

  // 替换后:
  std::unique_ptr<Thread> m_watch;
  ```

  实现修改 — watchDesktopThread 调用点 (行 176):
  ```cpp
  // 替换前:
  m_watch = new Thread(new TMethodJob<MSWindowsScreenSaver>(this, &MSWindowsScreenSaver::watchDesktopThread));

  // 替换后:
  m_watch = std::make_unique<Thread>(new TMethodJob<MSWindowsScreenSaver>(this, &MSWindowsScreenSaver::watchDesktopThread));
  ```

  实现修改 — watchProcess 调用点 (行 189):
  ```cpp
  // 替换前:
  m_watch = new Thread(new TMethodJob<MSWindowsScreenSaver>(this, &MSWindowsScreenSaver::watchProcessThread));

  // 替换后:
  m_watch = std::make_unique<Thread>(new TMethodJob<MSWindowsScreenSaver>(this, &MSWindowsScreenSaver::watchProcessThread));
  ```

  实现修改 — unwatchProcess (行 193-200):
  ```cpp
  // 替换前:
  if (m_watch != nullptr) {
    m_watch->cancel();
    m_watch->wait();
    delete m_watch;
    m_watch = nullptr;
  }

  // 替换后:
  if (m_watch) {
    m_watch->cancel();
    m_watch->wait();
    m_watch.reset();
  }
  ```

  **站点 2: MSWindowsDesks.cpp（行 761, 767-774）+ .h（行 183）**

  头文件修改 — Desk struct (MSWindowsDesks.h:183):
  ```cpp
  // 替换前:
  Thread *m_thread;

  // 替换后:
  std::unique_ptr<Thread> m_thread;
  ```

  实现修改 — addDesk (MSWindowsDesks.cpp:761):
  ```cpp
  // 替换前:
  desk->m_thread = new Thread(new TMethodJob<MSWindowsDesks>(this, &MSWindowsDesks::deskThread, desk));

  // 替换后:
  desk->m_thread = std::make_unique<Thread>(new TMethodJob<MSWindowsDesks>(this, &MSWindowsDesks::deskThread, desk));
  ```

  实现修改 — removeDesks (MSWindowsDesks.cpp:770-774):
  ```cpp
  // 替换前:
  desk->m_thread->wait();
  delete desk->m_thread;
  delete desk;

  // 替换后:
  desk->m_thread->wait();
  desk->m_thread.reset();  // unique_ptr 自动 delete Thread
  // desk 本身仍然是 raw new/delete（Desk* 在 map 中），保留 delete desk
  delete desk;
  ```

  **站点 3: OSXScreen.mm（行 158）+ .h（行 280）**

  头文件修改 (OSXScreen.h:280):
  ```cpp
  // 替换前:
  Thread *m_pmWatchThread;

  // 替换后:
  std::unique_ptr<Thread> m_pmWatchThread;
  ```

  实现修改 — 创建 (OSXScreen.mm:158):
  ```cpp
  // 替换前:
  m_pmWatchThread = new Thread(new TMethodJob<OSXScreen>(this, &OSXScreen::watchSystemPowerThread));

  // 替换后:
  m_pmWatchThread = std::make_unique<Thread>(new TMethodJob<OSXScreen>(this, &OSXScreen::watchSystemPowerThread));
  ```

  实现修改 — 析构 (OSXScreen.mm:200-201):
  ```cpp
  // 替换前:
  m_pmWatchThread->wait();
  delete m_pmWatchThread;

  // 替换后:
  m_pmWatchThread->wait();
  m_pmWatchThread.reset();
  ```

  **站点 4: PortalInputCapture.cpp（行 33）+ .h（行 43）**

  **前提检查：** 如果 Phase 3 已删除此文件（Linux 平台移除），跳过。

  头文件修改 (PortalInputCapture.h:43):
  ```cpp
  // 替换前:
  Thread *m_glibThread;

  // 替换后:
  std::unique_ptr<Thread> m_glibThread;
  ```

  实现修改 (PortalInputCapture.cpp:33):
  ```cpp
  // 替换前:
  m_glibThread = new Thread(tMethodJob);

  // 替换后:
  m_glibThread = std::make_unique<Thread>(tMethodJob);
  ```

  查找并替换 PortalInputCapture 中的 delete m_glibThread 为 m_glibThread.reset()。

  **站点 5: PortalRemoteDesktop.cpp（行 24）+ .h（行 90）**

  **前提检查：** 如果 Phase 3 已删除此文件，跳过。

  头文件修改 (PortalRemoteDesktop.h:90):
  ```cpp
  // 替换前:
  Thread *m_glibThread;

  // 替换后:
  std::unique_ptr<Thread> m_glibThread;
  ```

  实现修改 (PortalRemoteDesktop.cpp:24):
  ```cpp
  // 替换前:
  m_glibThread = new Thread(tMethodJob);

  // 替换后:
  m_glibThread = std::make_unique<Thread>(tMethodJob);
  ```

  查找并替换 PortalRemoteDesktop 中的 delete m_glibThread 为 m_glibThread.reset()。

  **头文件 include：** 每个修改的 .h 文件需确保 `#include <memory>` 已存在或添加。
  </action>

  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && grep -rn "new Thread" src/lib/platform/ | grep -v "make_unique" | grep -v "//" && echo "FAIL: raw new Thread found" || echo "PASS: no raw new Thread"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "new Thread" src/lib/platform/ | grep -v "make_unique" | grep -v "//"` 返回空（无 raw new Thread）
    - `grep -rn "delete m_watch\|delete m_pmWatchThread\|delete m_glibThread\|delete desk->m_thread" src/lib/platform/` 返回空（无 raw delete Thread）
    - `grep "unique_ptr<Thread>" src/lib/platform/MSWindowsScreenSaver.h` 包含 m_watch
    - `grep "unique_ptr<Thread>" src/lib/platform/MSWindowsDesks.h` 包含 m_thread
    - `grep "unique_ptr<Thread>" src/lib/platform/OSXScreen.h` 包含 m_pmWatchThread
    - `grep "make_unique<Thread>" src/lib/platform/MSWindowsScreenSaver.cpp` 出现 2 次
    - `grep "make_unique<Thread>" src/lib/platform/MSWindowsDesks.cpp` 出现 1 次
    - `grep "make_unique<Thread>" src/lib/platform/OSXScreen.mm` 出现 1 次
    - `grep "\.reset()" src/lib/platform/MSWindowsDesks.cpp` 出现在 removeDesks 中 m_thread.reset()
  </acceptance_criteria>

  <done>
  所有 6 个独立 raw new Thread 调用点替换为 unique_ptr<Thread> + make_unique（per D-01, D-02）：
  - MSWindowsScreenSaver: m_watch -> unique_ptr<Thread>，2 个赋值点 + unwatchProcess 改为 .reset()
  - MSWindowsDesks::Desk: m_thread -> unique_ptr<Thread>，addDesk + removeDesks 改为 .reset()
  - OSXScreen: m_pmWatchThread -> unique_ptr<Thread>，1 个赋值点 + 析构改为 .reset()
  - PortalInputCapture: m_glibThread -> unique_ptr<Thread>（如果文件仍存在）
  - PortalRemoteDesktop: m_glibThread -> unique_ptr<Thread>（如果文件仍存在）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Thread 生命周期 | Thread 析构必须在线程停止后（wait 之后），否则 ARCH->closeThread 可能死锁 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | Desk::m_thread 析构顺序 | mitigate | removeDesks 中保持 wait() -> reset() 顺序不变 |
| T-04-04 | Information Disclosure | Thread 清理不完整 | accept | unique_ptr 保证析构，Thread::~Thread() 已实现 ARCH->closeThread |
</threat_model>

<verification>
```bash
# 验证 platform 目录无 raw new Thread
grep -rn "new Thread" src/lib/platform/ | grep -v "make_unique" | grep -v "//"  # 应为空

# 验证无 raw delete Thread 成员
grep -rn "delete m_watch\|delete m_pmWatchThread\|delete m_glibThread\|delete .*m_thread" src/lib/platform/  # 应为空

# 验证 unique_ptr 替换
grep "unique_ptr<Thread>" src/lib/platform/MSWindowsScreenSaver.h  # m_watch
grep "unique_ptr<Thread>" src/lib/platform/MSWindowsDesks.h  # m_thread (in Desk struct)
grep "unique_ptr<Thread>" src/lib/platform/OSXScreen.h  # m_pmWatchThread
```
</verification>

<success_criteria>
1. 所有 platform 目录中的 raw new Thread 替换为 make_unique<Thread>
2. 所有 raw delete Thread* 替换为 .reset()
3. MSWindowsDesks::removeDesks 保持 wait() -> reset() -> delete desk 顺序
4. 头文件中 Thread* 成员全部改为 unique_ptr<Thread>
5. PortalInputCapture/PortalRemoteDesktop 条件处理（Phase 3 可能已删除）
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpp-modernization/04-02-SUMMARY.md`
</output>

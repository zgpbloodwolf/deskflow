---
phase: 04-cpp-modernization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/InputFilter.h
  - src/lib/server/InputFilter.cpp
  - src/lib/server/Config.cpp
autonomous: true
requirements: [MOD-04]

must_haves:
  truths:
    - "InputFilter::RestartServer::perform() 不再调用 exit(0)，改为投递 EventTypes::Quit 事件"
    - "RestartServer 拥有 IEventQueue* 成员来访问事件系统"
    - "Config::parseAction 创建 RestartServer 时传入 IEventQueue 指针"
  artifacts:
    - path: "src/lib/server/InputFilter.h"
      provides: "RestartServer 带 IEventQueue* 成员和构造函数参数"
      contains: "IEventQueue"
    - path: "src/lib/server/InputFilter.cpp"
      provides: "perform() 使用 m_events->addEvent(Event(EventTypes::Quit))"
      contains: "EventTypes::Quit"
    - path: "src/lib/server/Config.cpp"
      provides: "parseAction 创建 RestartServer 时传入 m_events"
      contains: "RestartServer"
  key_links:
    - from: "src/lib/server/InputFilter.cpp:RestartServer::perform()"
      to: "EventTypes::Quit"
      via: "m_events->addEvent(Event(EventTypes::Quit))"
      pattern: "EventTypes::Quit"
    - from: "src/lib/server/Config.cpp:parseAction()"
      to: "RestartServer(IEventQueue*, Mode)"
      via: "new InputFilter::RestartServer(m_events, mode)"
      pattern: "new InputFilter::RestartServer"
---

<objective>
将 InputFilter::RestartServer::perform() 中的 exit(0) 替换为优雅的 EventTypes::Quit 事件投递（per D-12, D-13, D-14）。

Purpose: exit(0) 强制终止进程，跳过所有 RAII 析构和资源清理。替换为 EventTypes::Quit 允许 EventQueue::loop() 正常退出，保证资源正确释放。

Output: InputFilter::RestartServer 使用事件驱动退出，exit(0) 调用被移除。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cpp-modernization/04-CONTEXT.md
@.planning/phases/04-cpp-modernization/04-RESEARCH.md

<interfaces>
<!-- RestartServer 当前声明（无 IEventQueue*） -->
<!-- 来源: src/lib/server/InputFilter.h:155-174 -->
class RestartServer : public Action {
public:
  enum Mode { restart };
  explicit RestartServer(Mode = restart);
  Mode getMode() const;
  Action *clone() const override;
  std::string format() const override;
  void perform(const Event &) override;
private:
  Mode m_mode;
};

<!-- 参考: 其他 Action 子类都有 IEventQueue* 成员 -->
// LockCursorToScreenAction(IEventQueue *events, Mode mode)
// SwitchToScreenAction(IEventQueue *events, const std::string &screen)
// KeyboardBroadcastAction(IEventQueue *events, Mode mode)
// 所有都有 IEventQueue *m_events 成员

<!-- ServerApp 使用 EventTypes::Quit 的参考模式 -->
<!-- 来源: src/lib/deskflow/ServerApp.cpp:164-167 -->
getEvents()->addEvent(Event(EventTypes::Quit));

<!-- Config 类有 IEventQueue* m_events 成员 -->
<!-- 来源: src/lib/server/Config.h:477 -->
IEventQueue *m_events;

<!-- Config.cpp 创建 RestartServer 的位置 -->
<!-- 来源: src/lib/server/Config.cpp:1112 -->
action = new InputFilter::RestartServer(mode);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace InputFilter::RestartServer exit(0) with EventTypes::Quit event</name>
  <files>
    src/lib/server/InputFilter.h,
    src/lib/server/InputFilter.cpp,
    src/lib/server/Config.cpp
  </files>
  <read_first>
    src/lib/server/InputFilter.h (RestartServer 类声明:155-174，对比其他 Action 子类)
    src/lib/server/InputFilter.cpp (RestartServer 实现:240-266)
    src/lib/server/Config.cpp (parseAction 创建 RestartServer:1097-1113)
    src/lib/deskflow/ServerApp.cpp (EventTypes::Quit 参考用法，约行 164-167)
  </read_first>

  <action>
  **1. 头文件 InputFilter.h 修改 — RestartServer 类（行 155-174）：**

  修改构造函数签名，添加 IEventQueue* 参数，添加 m_events 成员：
  ```cpp
  // 替换前:
  class RestartServer : public Action
  {
  public:
    enum Mode
    {
      restart
    };

    explicit RestartServer(Mode = restart);

    Mode getMode() const;

    // Action overrides
    Action *clone() const override;
    std::string format() const override;
    void perform(const Event &) override;

  private:
    Mode m_mode;
  };

  // 替换后:
  class RestartServer : public Action
  {
  public:
    enum Mode
    {
      restart
    };

    explicit RestartServer(IEventQueue *events, Mode = restart);

    Mode getMode() const;

    // Action overrides
    Action *clone() const override;
    std::string format() const override;
    void perform(const Event &) override;

  private:
    Mode m_mode;
    IEventQueue *m_events;
  };
  ```

  **2. 实现文件 InputFilter.cpp 修改 — RestartServer 实现（行 240-266）：**

  构造函数（行 240-243）：
  ```cpp
  // 替换前:
  InputFilter::RestartServer::RestartServer(Mode mode) : m_mode(mode)
  {
    // do nothing
  }

  // 替换后:
  InputFilter::RestartServer::RestartServer(IEventQueue *events, Mode mode)
      : m_mode(mode),
        m_events(events)
  {
    // do nothing
  }
  ```

  clone()（行 250-253）：
  ```cpp
  // 替换前:
  InputFilter::Action *InputFilter::RestartServer::clone() const
  {
    return new RestartServer(*this);
  }

  // 替换后:
  InputFilter::Action *InputFilter::RestartServer::clone() const
  {
    return new RestartServer(m_events, m_mode);
  }
  ```

  perform()（行 262-266）：
  ```cpp
  // 替换前:
  void InputFilter::RestartServer::perform(const Event &)
  {
    // HACK Super hack we should gracefully exit
    exit(0);
  }

  // 替换后:
  void InputFilter::RestartServer::perform(const Event &)
  {
    LOG_DEBUG("restart server via graceful quit event");
    m_events->addEvent(Event(EventTypes::Quit));
  }
  ```

  移除 `#include <cstdlib>` 中的 `exit` 使用（如果 InputFilter.cpp 中只有 RestartServer 使用 exit(0)，则移除 `<cstdlib>` include。但如果文件其他地方使用了cstdlib函数则保留）。

  **3. Config.cpp 修改 — parseAction（行 1112）：**

  ```cpp
  // 替换前:
  action = new InputFilter::RestartServer(mode);

  // 替换后:
  action = new InputFilter::RestartServer(m_events, mode);
  ```

  Config 类已有 `IEventQueue *m_events` 成员（Config.h:477），可直接使用。
  </action>

  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && grep "exit(0)" src/lib/server/InputFilter.cpp | grep -v "//" && echo "FAIL: exit(0) still present" || echo "PASS: exit(0) removed"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "exit(0)" src/lib/server/InputFilter.cpp | grep -v "//"` 返回空（exit(0) 已移除）
    - `grep "IEventQueue \*m_events" src/lib/server/InputFilter.h` 匹配 RestartServer 类中的成员
    - `grep "explicit RestartServer(IEventQueue" src/lib/server/InputFilter.h` 存在
    - `grep "EventTypes::Quit" src/lib/server/InputFilter.cpp` 出现在 perform() 中
    - `grep "new InputFilter::RestartServer(m_events" src/lib/server/Config.cpp` 存在
    - `grep "new InputFilter::RestartServer(mode)" src/lib/server/Config.cpp` 返回空（旧签名已替换）
    - CoreArgParser.cpp 和 ArchMiscWindows.cpp 中的 exit() 保持不变（per D-13, D-14）
  </acceptance_criteria>

  <done>
  InputFilter::RestartServer::perform() 中的 exit(0) 替换为 m_events->addEvent(Event(EventTypes::Quit))（per D-12）。RestartServer 添加 IEventQueue* m_events 成员和构造函数参数，Config::parseAction 传入 m_events。CoreArgParser.cpp 和 ArchMiscWindows.cpp 的 exit() 保持不变（per D-13, D-14）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RestartServer -> EventQueue | 通过 IEventQueue* 投递 Quit 事件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Denial of Service | Quit 事件投递 | accept | 正常退出机制，与 ServerApp 等现有 Quit 用法一致 |
</threat_model>

<verification>
```bash
# 验证 exit(0) 已移除
grep "exit(0)" src/lib/server/InputFilter.cpp | grep -v "//"  # 应为空

# 验证 EventTypes::Quit 替换
grep "EventTypes::Quit" src/lib/server/InputFilter.cpp  # 应在 perform() 中

# 验证 IEventQueue 成员
grep "IEventQueue" src/lib/server/InputFilter.h | grep RestartServer -A5  # 应有 m_events

# 验证 Config.cpp 传入 m_events
grep "new InputFilter::RestartServer" src/lib/server/Config.cpp  # 应为 (m_events, mode)

# 验证保留的 exit() 未被修改
grep "exit(" src/lib/deskflow/CoreArgParser.cpp  # 应仍存在（per D-13）
```
</verification>

<success_criteria>
1. InputFilter.cpp 中 RestartServer::perform() 不再包含 exit(0)
2. RestartServer 拥有 IEventQueue* m_events 成员
3. perform() 投递 Event(EventTypes::Quit) 事件
4. Config.cpp 创建 RestartServer 时传入 m_events
5. clone() 使用新构造函数签名
6. CoreArgParser.cpp 和 ArchMiscWindows.cpp 的 exit() 未被修改
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpp-modernization/04-04-SUMMARY.md`
</output>

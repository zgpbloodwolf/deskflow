---
phase: 04-cpp-modernization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/base/EventQueue.h
  - src/lib/base/EventQueue.cpp
autonomous: true
requirements: [MOD-01]

must_haves:
  truths:
    - "EventQueue 的 m_readyMutex 和 m_readyCondVar 使用 unique_ptr 管理"
    - "EventQueue 析构函数无手动 delete"
    - "所有 m_readyMutex 使用点改为 m_readyMutex.get() 或使用 unique_ptr 的 -> 操作符"
  artifacts:
    - path: "src/lib/base/EventQueue.h"
      provides: "unique_ptr<Mutex> m_readyMutex 和 unique_ptr<CondVar<bool>> m_readyCondVar 成员"
      contains: "unique_ptr"
    - path: "src/lib/base/EventQueue.cpp"
      provides: "make_unique 构造，自动析构，无 raw delete"
      min_lines: 40
  key_links:
    - from: "src/lib/base/EventQueue.cpp:构造函数"
      to: "std::make_unique"
      via: "m_readyMutex 和 m_readyCondVar 的构造"
      pattern: "make_unique"
    - from: "src/lib/base/EventQueue.cpp:loop()"
      to: "m_readyMutex"
      via: "Lock lock(m_readyMutex.get())"
      pattern: "Lock.*m_readyMutex"
---

<objective>
将 EventQueue 的 m_readyMutex* 和 m_readyCondVar* raw 指针成员替换为 unique_ptr（per D-08, D-09）。

Purpose: EventQueue 是全局核心组件，其成员指针是 raw new/delete 管理的残留。替换为 unique_ptr 消除手动 delete 风险，保证析构顺序安全。

Output: EventQueue.h/cpp 中 m_readyMutex 和 m_readyCondVar 使用 unique_ptr，无 raw delete。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cpp-modernization/04-CONTEXT.md
@.planning/phases/04-cpp-modernization/04-RESEARCH.md

<interfaces>
<!-- EventQueue 当前 raw 指针成员 -->
<!-- 来源: src/lib/base/EventQueue.h:121-122 -->
Mutex *m_readyMutex = nullptr;
CondVar<bool> *m_readyCondVar = nullptr;

<!-- Lock 类接口 -->
<!-- 来源: src/lib/mt/Lock.h -->
explicit Lock(const Mutex *mutex);    // 需传 Mutex*
explicit Lock(const CondVarBase *cv); // 需传 CondVarBase*

<!-- EventQueue 构造/析构 -->
<!-- 来源: src/lib/base/EventQueue.cpp:32-46 -->
EventQueue::EventQueue()
    : m_readyMutex(new Mutex),
      m_readyCondVar(new CondVar<bool>(m_readyMutex, false))
~EventQueue() {
  delete m_readyCondVar;
  delete m_readyMutex;
}

<!-- 使用点 -->
// loop() 行 52: Lock lock(m_readyMutex);
// loop() 行 53: *m_readyCondVar = true;
// loop() 行 54: m_readyCondVar->signal();
// addEvent() 行 198: if (!(*m_readyCondVar))
// waitForReady() 行 411: Lock lock(m_readyMutex);
// waitForReady() 行 413: m_readyCondVar->wait()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace EventQueue m_readyMutex and m_readyCondVar raw pointers with unique_ptr</name>
  <files>src/lib/base/EventQueue.h, src/lib/base/EventQueue.cpp</files>
  <read_first>
    src/lib/base/EventQueue.h (m_readyMutex, m_readyCondVar 成员声明)
    src/lib/base/EventQueue.cpp (构造函数、析构函数、loop()、addEvent()、waitForReady())
    src/lib/mt/Lock.h (Lock 构造函数接受 Mutex* 参数)
  </read_first>

  <action>
  **头文件 EventQueue.h 修改：**

  确保 `#include <memory>` 已存在（当前已有，行 18: `#include <memory>`）。

  替换成员声明（行 121-122）：
  ```cpp
  // 替换前:
  Mutex *m_readyMutex = nullptr;
  CondVar<bool> *m_readyCondVar = nullptr;

  // 替换后:
  std::unique_ptr<Mutex> m_readyMutex;
  std::unique_ptr<CondVar<bool>> m_readyCondVar;
  ```

  **实现文件 EventQueue.cpp 修改：**

  构造函数（行 32-33）：
  ```cpp
  // 替换前:
  EventQueue::EventQueue() : m_readyMutex(new Mutex), m_readyCondVar(new CondVar<bool>(m_readyMutex, false))

  // 替换后:
  EventQueue::EventQueue()
      : m_readyMutex(std::make_unique<Mutex>()),
        m_readyCondVar(std::make_unique<CondVar<bool>>(m_readyMutex.get(), false))
  ```

  析构函数（行 39-46）：
  ```cpp
  // 替换前:
  EventQueue::~EventQueue()
  {
    delete m_readyCondVar;
    delete m_readyMutex;
    ARCH->setSignalHandler(...);
  }

  // 替换后:
  EventQueue::~EventQueue()
  {
    // unique_ptr 自动析构 m_readyCondVar 和 m_readyMutex
    // 析构顺序与声明顺序相反（C++ 标准）：先 m_readyCondVar，再 m_readyMutex
    // CondVar 先于 Mutex 析构是安全的（CondVar 不持有 Mutex 引用）
    ARCH->setSignalHandler(Arch::ThreadSignal::Interrupt, nullptr, nullptr);
    ARCH->setSignalHandler(Arch::ThreadSignal::Terminate, nullptr, nullptr);
  }
  ```

  使用点修改 — loop()（行 52）：
  ```cpp
  // 替换前:
  Lock lock(m_readyMutex);

  // 替换后:
  Lock lock(m_readyMutex.get());
  ```

  注意: loop() 中 `*m_readyCondVar = true` 和 `m_readyCondVar->signal()` 不需要修改——
  unique_ptr 的 operator* 和 operator-> 会自动委托给底层指针。

  使用点修改 — addEvent()（行 198）：
  ```cpp
  // 替换前:
  } else if (!(*m_readyCondVar)) {

  // 无需修改: unique_ptr 的 operator* 返回 CondVar<bool>&，! 操作符正常工作
  ```

  使用点修改 — waitForReady()（行 411）：
  ```cpp
  // 替换前:
  Lock lock(m_readyMutex);

  // 替换后:
  Lock lock(m_readyMutex.get());
  ```

  waitForReady() 中 `m_readyCondVar->wait()` 不需要修改。

  **总结所有需修改的 Lock 调用：**
  - 行 52 (loop): `Lock lock(m_readyMutex)` -> `Lock lock(m_readyMutex.get())`
  - 行 411 (waitForReady): `Lock lock(m_readyMutex)` -> `Lock lock(m_readyMutex.get())`

  其余使用 unique_ptr 的 operator* 或 operator-> 的地方无需改动。
  </action>

  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && grep -c "delete m_ready" src/lib/base/EventQueue.cpp | grep -q '^0$' && echo "PASS: no raw delete" || echo "FAIL: raw delete found"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "delete m_ready" src/lib/base/EventQueue.cpp` 返回空（无 raw delete）
    - `grep "unique_ptr<Mutex>" src/lib/base/EventQueue.h` 匹配 m_readyMutex
    - `grep "unique_ptr<CondVar" src/lib/base/EventQueue.h` 匹配 m_readyCondVar
    - `grep "make_unique" src/lib/base/EventQueue.cpp` 包含 Mutex 和 CondVar
    - `grep "m_readyMutex.get()" src/lib/base/EventQueue.cpp` 至少出现 2 次（构造函数 CondVar 参数 + Lock 调用）
    - `grep "new Mutex\|new CondVar" src/lib/base/EventQueue.cpp` 返回空（无 raw new）
  </acceptance_criteria>

  <done>
  EventQueue 的 m_readyMutex 和 m_readyCondVar 替换为 unique_ptr（per D-08, D-09），构造函数使用 make_unique，析构函数无手动 delete，Lock 调用使用 .get()。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EventQueue -> Lock | Lock 接受 Mutex*，unique_ptr 后需 .get() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | 析构顺序 | accept | C++ 保证成员按声明逆序析构，CondVar 先于 Mutex，安全 |
</threat_model>

<verification>
```bash
# 验证无 raw delete
grep "delete m_ready" src/lib/base/EventQueue.cpp  # 应为空

# 验证 unique_ptr
grep "unique_ptr" src/lib/base/EventQueue.h  # 应有 m_readyMutex 和 m_readyCondVar

# 验证 make_unique
grep "make_unique" src/lib/base/EventQueue.cpp  # 应有 Mutex 和 CondVar

# 验证 .get() 调用
grep "m_readyMutex.get()" src/lib/base/EventQueue.cpp  # 应有 2+ 次
```
</verification>

<success_criteria>
1. EventQueue.h 中 m_readyMutex 和 m_readyCondVar 为 unique_ptr
2. EventQueue.cpp 析构函数无手动 delete
3. 构造函数使用 std::make_unique
4. Lock 调用使用 .get() 获取 Mutex*
5. CondVool<bool> 的 operator* 和 -> 使用点无需修改
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpp-modernization/04-03-SUMMARY.md`
</output>

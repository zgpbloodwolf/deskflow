---
phase: 05-build-validation-regression
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - cmake/vcpkg.json.in
autonomous: false
requirements: [BQ-01]

user_setup:
  - service: "Windows Physical Machine"
    why: "Windows build environment configuration and build verification (per D-01, D-02, D-03, D-04)"
    env_vars: []
    dashboard_config:
      - task: "Install Visual Studio Build Tools with MSVC v14+ C++ workload"
        location: "https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio"
      - task: "Install CMake 3.24+ (via Visual Studio or standalone)"
        location: "https://cmake.org/download/"
      - task: "Clone the repository and checkout the branch with Phase 5 changes"
        location: "git clone && git checkout <branch>"
      - task: "Configure and build: cmake -S . -B build -G \"Visual Studio 17 2022\" -A x64 -DVCPKG_QT=ON -DBUILD_TESTS=ON -DSKIP_BUILD_TESTS=ON && cmake --build build --config Release"
        location: "Developer Command Prompt for VS"

must_haves:
  truths:
    - "Windows (MSVC) clean build succeeds with zero errors"
    - "Windows vcpkg.json is generated without OpenSSL dependency"
    - "Windows build produces deskflow-core.exe and deskflow.exe"
  artifacts:
    - path: "vcpkg.json"
      provides: "Generated Windows vcpkg manifest"
    - path: "build/bin/Release/deskflow-core.exe"
      provides: "Windows core executable"
  key_links:
    - from: "CMakeLists.txt"
      to: "vcpkg.json"
      via: "configure_file(cmake/vcpkg.json.in)"
      pattern: "configure_file.*vcpkg"
---

<objective>
在 Windows 物理机上配置构建环境、执行构建并修复问题。

Purpose: 验证 Phase 1-4 的代码改动在 Windows (MSVC) 上也能正确编译。由于 Windows 构建环境尚未配置，这需要人工参与安装依赖和执行构建。
Output: Windows 上零错误的 Release 构建，生成的可执行文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-build-validation-regression/05-CONTEXT.md
@.planning/phases/05-build-validation-regression/05-RESEARCH.md
@.planning/phases/05-build-validation-regression/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare Windows build prerequisites and code sync</name>
  <files>cmake/vcpkg.json.in, CMakeLists.txt</files>
  <read_first>
    cmake/vcpkg.json.in
    CMakeLists.txt
    cmake/Libraries.cmake
  </read_first>
  <acceptance_criteria>
    - grep -c "openssl" cmake/vcpkg.json.in 返回 0（Plan 01 已修复，确认状态）
    - git log 中有 Plan 01 的提交记录
    - 代码已推送到远程仓库
  </acceptance_criteria>
  <action>
**此任务在 Mac 上执行，为 Windows 构建做准备 (per D-03, D-04)。**

**步骤 1: 确认 Plan 01 的修改已提交**

```bash
# 确认 vcpkg.json.in 不再包含 openssl
grep "openssl" cmake/vcpkg.json.in || echo "OK: no openssl reference"
grep "gtest" cmake/vcpkg.json.in || echo "OK: no gtest reference"
```

**步骤 2: 检查 Windows 构建所需的代码是否完整**

读取 CMakeLists.txt 确认 Windows 平台相关配置：
- `REQUIRED_MSVC_RUNTIME_MAJOR 14` 已定义
- `VCPKG_QT` option 已定义
- `configure_file(cmake/vcpkg.json.in ...)` 逻辑存在

读取 `cmake/Libraries.cmake` 确认：
- Windows 平台的库查找路径
- `_CRT_SECURE_NO_WARNINGS` 定义（已知问题，per CONCERNS.md）
- Qt 依赖配置

**步骤 3: 推送代码到远程仓库**

```bash
git push origin main
# 或者当前工作分支
```

**步骤 4: 生成 Windows 构建指导文件**

创建一个简洁的构建指导（不提交到仓库，仅输出到终端）：

Windows 构建命令序列：
```
1. 安装 Visual Studio 2022 Build Tools (MSVC v14+ C++ workload)
2. 安装 CMake 3.24+ (如果 VS 未包含)
3. git clone <repo> && git checkout <branch>
4. Developer Command Prompt:
   cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DVCPKG_QT=ON -DBUILD_TESTS=ON -DSKIP_BUILD_TESTS=ON
   cmake --build build --config Release
5. ctest --test-dir build/src/unittests -C Release --output-on-failure
6. build\bin\Release\legacytests.exe
```
  </action>
  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && grep -c "openssl" cmake/vcpkg.json.in; git log --oneline -3</automated>
  </verify>
  <done>
    - vcpkg.json.in 确认无 openssl 引用
    - 所有修改已推送到远程
    - Windows 构建命令序列已提供给用户
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Windows 构建环境配置和构建验证（需要用户在 Windows 物理机上操作，per D-01, D-04）</what-built>
  <how-to-verify>
    在 Windows 物理机上执行以下步骤：

    1. 安装 Visual Studio 2022 Build Tools（如果尚未安装）
       - 选择 "Desktop development with C++" workload
       - 确认 MSVC v14+ 编译器安装完成

    2. 安装 CMake 3.24+（如果 VS 未包含足够新版本）

    3. 打开 "Developer Command Prompt for VS 2022"

    4. 克隆代码（如果尚未克隆）
       ```
       git clone <repo-url>
       cd deskflow
       git pull
       ```

    5. 配置和构建
       ```
       cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DVCPKG_QT=ON -DBUILD_TESTS=ON -DSKIP_BUILD_TESTS=ON
       cmake --build build --config Release
       ```

    6. 验证构建结果
       - 构建应以 exit code 0 完成
       - 确认 `build\bin\Release\deskflow-core.exe` 存在
       - 确认 `build\bin\Release\deskflow.exe` 存在（或 Deskflow GUI）
       - 检查构建输出中无项目源码编译错误

    7. 如果构建失败，将错误信息复制回来，Claude 会分析并提供修复

    8. 如果构建成功，运行测试：
       ```
       ctest --test-dir build/src/unittests -C Release --output-on-failure
       build\bin\Release\legacytests.exe
       ```

    预期结果：
    - 构建零错误完成
    - 所有测试通过
    - 两个可执行文件生成
  </how-to-verify>
  <acceptance_criteria>
    用户在输入 "approved" 之前必须确认以下条件全部满足：
    - "build\\bin\\Release\\deskflow-core.exe" 文件存在且大小 > 0
    - GUI 可执行文件（deskflow.exe 或 Deskflow.exe）存在
    - 构建输出中无项目源码 error: 行
    - ctest 命令报告 100% tests passed
    - legacytests.exe 输出中无 [FAILED] 行
  </acceptance_criteria>
  <resume-signal>输入 "approved" 确认以上 5 个条件全部满足，或粘贴构建/测试错误信息</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code -> Windows compiler | 跨平台代码在 MSVC 上可能有不同行为 |
| vcpkg -> External dependencies | vcpkg 从网络下载依赖，需确保供应链安全 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | vcpkg dependencies | accept | vcpkg 使用 git baseline 锁定版本，风险可控 |
| T-05-05 | I | Windows build output | accept | 构建在本地物理机，无远程暴露 |
| T-05-06 | D | MSVC compiler warnings | mitigate | 验证构建输出无项目代码警告 (per D-11) |
</threat_model>

<verification>
1. Windows 构建以 exit code 0 完成
2. build\bin\Release\deskflow-core.exe 存在
3. build\bin\Release\deskflow.exe 存在
4. ctest 所有测试通过
5. legacytests.exe 所有测试通过
</verification>

<success_criteria>
- Windows (MSVC) clean build 以 exit 0 完成
- Windows 上所有自动化测试通过
- 两个平台（macOS + Windows）的构建和测试都已验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-build-validation-regression/05-02-SUMMARY.md`
</output>

---
phase: 05-build-validation-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmake/vcpkg.json.in
  - src/unittests/net/CMakeLists.txt
  - CMakeLists.txt
autonomous: true
requirements: [BQ-01, BQ-02]

must_haves:
  truths:
    - "macOS (clang) clean build succeeds with zero errors and zero project-code warnings"
    - "All modern Qt Test tests and legacy Google Test tests pass on macOS"
    - "vcpkg.json.in no longer references OpenSSL"
    - "Stale TLS test registrations in net/CMakeLists.txt are cleaned up"
  artifacts:
    - path: "build/bin/deskflow-core"
      provides: "Core client/server executable"
    - path: "build/bin/Deskflow.app"
      provides: "GUI application bundle"
    - path: "cmake/vcpkg.json.in"
      provides: "Windows vcpkg manifest without OpenSSL"
  key_links:
    - from: "CMakeLists.txt"
      to: "cmake/vcpkg.json.in"
      via: "configure_file for Windows builds"
      pattern: "configure_file.*vcpkg\\.json\\.in"
---

<objective>
修复 macOS 上的构建问题并确保所有自动化测试通过，为 Windows 构建准备 vcpkg 模板。

Purpose: Phase 5 的第一阶段，在 macOS 上建立零错误零警告的构建基线，并确保所有自动化测试通过。这为后续 Windows 构建和手动冒烟测试提供坚实基础。
Output: macOS 上干净构建通过、所有测试通过、vcpkg 模板已清理
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/05-build-validation-regression/05-CONTEXT.md
@.planning/phases/05-build-validation-regression/05-RESEARCH.md

<interfaces>
<!-- 构建系统关键入口 -->

从 CMakeLists.txt:
  cmake_minimum_required(VERSION 3.24)
  set(CMAKE_CXX_STANDARD 20)
  set(REQUIRED_QT_VERSION 6.7.0)

从 cmake/vcpkg.json.in (当前内容):
  "dependencies": ["gtest", "openssl", @QT_LIBS@]

从 src/unittests/net/CMakeLists.txt (当前内容):
  # 空文件 -- Phase 2 删除 TLS 测试后为空，但应确认

从 src/unittests/CMakeLists.txt:
  function(create_test ...) -- 测试注册函数
  add_subdirectory(net) -- 包含 net 测试

构建命令 (macOS):
  cmake -S . -B build -DCMAKE_EXE_LINKER_FLAGS="-F/tmp" -DCMAKE_SHARED_LINKER_FLAGS="-F/tmp" -DBUILD_TESTS=ON -DSKIP_BUILD_TESTS=ON
  cmake --build build
  ctest --test-dir build/src/unittests --output-on-failure
  ./build/bin/legacytests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix macOS build issues and clean up stale references</name>
  <files>cmake/vcpkg.json.in, src/unittests/net/CMakeLists.txt, CMakeLists.txt</files>
  <read_first>
    cmake/vcpkg.json.in
    src/unittests/net/CMakeLists.txt
    CMakeLists.txt
    cmake/Libraries.cmake
  </read_first>
  <behavior>
    - vcpkg.json.in 中移除 "openssl" 和 "gtest" 依赖后文件仍为有效 JSON
    - net/CMakeLists.txt 确认为空（无残留 TLS 测试注册）
    - macOS clean build 命令以 exit code 0 完成
    - 构建输出中无项目源码的 warning: 行（grep -v 'third-party' 后计数为 0）
  </behavior>
  <action>
**步骤 1: 清理 vcpkg.json.in 中的 OpenSSL 引用 (per D-03, RESEARCH Pitfall 6)**

编辑 `cmake/vcpkg.json.in`，从 dependencies 数组中移除 `"openssl"` 和 `"gtest"` 这两个条目。
Phase 2 已移除 OpenSSL 依赖，gtest 在 Windows 上通过 FetchContent 获取（与 macOS 相同）。
修改后的 dependencies 数组应只包含 `@QT_LIBS@`。

**步骤 2: 验证 net/CMakeLists.txt 状态 (per D-05, RESEARCH Pitfall 2)**

读取 `src/unittests/net/CMakeLists.txt` 确认其内容。如果只有 SPDX 头注释和空行，则无需修改。
如果有残留的 TLS 测试注册（SecureUtilsTests、FingerprintTests、FingerprintDatabaseTests），则删除这些注册。

**步骤 3: 执行 macOS clean build**

```bash
# 删除旧构建目录（避免 stale cache，per RESEARCH Pitfall 2）
rm -rf build

# 创建 AGL stub（macOS 26+ workaround，per RESEARCH Pitfall 1）
mkdir -p /tmp/AGL.framework/Versions/A/Resources
echo 'void AGLStubFunction(void) {}' > /tmp/AGL.c
cc -dynamiclib -o /tmp/AGL.framework/Versions/A/AGL /tmp/AGL.c \
  -install_name /System/Library/Frameworks/AGL.framework/Versions/A/AGL \
  -compatability_version 1.0.0 -current_version 1.0.0
ln -sf A /tmp/AGL.framework/Versions/Current
ln -sf Versions/Current/AGL /tmp/AGL.framework/AGL
ln -sf Versions/Current/Resources /tmp/AGL.framework/Resources

# 配置
cmake -S . -B build \
  -DCMAKE_EXE_LINKER_FLAGS="-F/tmp" \
  -DCMAKE_SHARED_LINKER_FLAGS="-F/tmp" \
  -DBUILD_TESTS=ON \
  -DSKIP_BUILD_TESTS=ON

# 构建
cmake --build build 2>&1 | tee build.log
```

**步骤 4: 分析构建输出**

检查 build.log 中是否存在项目源码的编译错误或警告：
```bash
# 检查编译错误
grep -c "error:" build.log || true

# 检查项目源码警告（排除第三方库路径如 vendor/、_deps/、Qt框架）
grep "warning:" build.log | grep -v 'vendor/' | grep -v '_deps/' | grep -v 'ranlib' || true
```

**步骤 5: 如果存在编译错误或项目警告，逐一修复**

常见预期问题（基于 RESEARCH.md）：
- ranlib "has no symbols" 警告：这些是无害的（X11 stubs + MOC），per RESEARCH Pitfall 4，接受这些警告
- AGL framework not found：确保 AGL stub 已正确创建
- Phase 3/4 改动引入的编译问题：根据具体错误修复

如果构建成功（exit 0），验证生成的二进制文件存在：
```bash
ls -la build/bin/deskflow-core build/bin/Deskflow.app/Contents/MacOS/Deskflow 2>/dev/null || true
```
  </action>
  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && cmake --build build 2>&1 | tail -5 && echo "EXIT_CODE=$?"</automated>
  </verify>
  <done>
    - cmake/vcpkg.json.in 中不包含 "openssl" 和 "gtest" 字符串
    - src/unittests/net/CMakeLists.txt 无 TLS 测试注册
    - macOS clean build 以 exit code 0 完成
    - build.log 中无项目源码编译错误（error:）
    - 二进制文件 build/bin/deskflow-core 存在
  </done>
</task>

<task type="auto">
  <name>Task 2: Run and fix all automated tests on macOS</name>
  <files>src/unittests/platform/OSXKeyStateTests.cpp</files>
  <read_first>
    src/unittests/platform/OSXKeyStateTests.cpp
    src/unittests/platform/OSXKeyStateTests.h
  </read_first>
  <behavior>
    - ctest 命令 100% 通过（所有注册的测试）
    - legacytests 可执行文件所有测试通过
    - OSXKeyStateTests 如果因键盘布局依赖失败，需分析是否为 Phase 1-4 引入的回归
  </behavior>
  <action>
**步骤 1: 运行所有现代测试 (Qt Test)**

```bash
ctest --test-dir build/src/unittests --output-on-failure 2>&1 | tee test-results.log
```

**步骤 2: 运行 legacy 测试 (Google Test)**

```bash
./build/bin/legacytests 2>&1 | tee legacy-test-results.log
```

**步骤 3: 分析测试结果**

检查测试通过率：
```bash
# 现代测试
grep -E "tests passed|tests failed|FAIL" test-results.log || true

# Legacy 测试
grep -E "PASSED|FAILED|\\[  PASSED  \\]|\\[  FAILED  \\]" legacy-test-results.log || true
```

**步骤 4: 如果 OSXKeyStateTests 失败，调查根因 (per RESEARCH Open Question 1)**

读取 `src/unittests/platform/OSXKeyStateTests.cpp`，分析失败的 3 个测试方法：
- `fakePollCharWithModifier` -- 键盘修饰键映射测试
- `mapKeyFromOSX_OSXKey` -- macOS 按键到通用按键映射
- `mapKeyShift` -- Shift 键映射

判断依据：
1. 检查测试代码是否引用了 Phase 1-4 修改过的文件（AsioTCPSocket, SPSC buffers, Settings TLS removal 等）
2. 如果测试代码不涉及 Phase 1-4 修改的模块，则是预先存在的问题（键盘布局依赖，per RESEARCH Pitfall 5）
3. 如果是预先存在的问题，在测试文件中添加条件跳过（使用 QSKIP 或条件判断），并添加注释说明原因
4. 如果是回归问题，修复被测代码使测试通过

**重要 (per D-07): 不要删除失败的测试，不要修改测试断言使其通过。如果是预先存在的布局依赖问题，使用 QSKIP 跳过并标注原因。**

**步骤 5: 如果其他测试失败，逐一修复**

根据 RESEARCH.md，Phase 3 和 Phase 4 尚未执行，所以：
- ArchNetworkBSDTests.cpp 是 Unix-only 的 legacy 测试，依赖 POSIX 头文件，在 macOS 上应该通过
- 如果 D-06 提到的 std::jthread 更新已执行（Phase 4），legacy 测试中引用旧 Thread 类的代码需要更新
- 由于 Phase 3/4 尚未执行，预期所有现有测试应该通过（OSXKeyStateTests 布局依赖除外）

**步骤 6: 修复后重新运行全部测试验证**

```bash
ctest --test-dir build/src/unittests --output-on-failure
./build/bin/legacytests
```
  </action>
  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && ctest --test-dir build/src/unittests --output-on-failure 2>&1 | tail -10 && ./build/bin/legacytests 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ctest 所有注册测试通过（100% pass rate，OSXKeyStateTests 布局依赖测试除外如果已用 QSKIP 标记）
    - legacytests 所有 27 个测试通过
    - 测试输出中无 FAIL 标记（QSKIP 标记除外）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build system -> Compiler | CMake 配置传递编译选项，不受信的编译标志可能导致安全风险 |
| Test execution -> File system | 测试可能读写临时文件，需确保不泄露敏感信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S | vcpkg.json.in | accept | 模板文件由 CMake 处理，不直接面向用户，低风险 |
| T-05-02 | T | Build output | accept | 构建输出在本地机器，无远程暴露 |
| T-05-03 | I | AGL stub library | accept | 临时 stub 在 /tmp，系统重启后清除，不持久化 |
</threat_model>

<verification>
1. `cmake/vcpkg.json.in` 不包含 "openssl" 或 "gtest" 字符串
2. `cmake --build build` 以 exit code 0 完成
3. `ctest --test-dir build/src/unittests` 所有测试通过
4. `./build/bin/legacytests` 所有测试通过
5. 项目源码零编译警告（排除 ranlib 和第三方库警告）
</verification>

<success_criteria>
- macOS clean build 以 exit 0 完成，零项目代码编译错误
- 所有自动化测试通过（OSXKeyStateTests 键盘布局依赖测试可 QSKIP）
- vcpkg.json.in 不再引用已移除的 OpenSSL
</success_criteria>

<output>
After completion, create `.planning/phases/05-build-validation-regression/05-01-SUMMARY.md`
</output>

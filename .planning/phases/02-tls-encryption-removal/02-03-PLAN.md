---
phase: 02-tls-encryption-removal
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/common/Settings.h
  - src/lib/common/Settings.cpp
  - src/lib/gui/MainWindow.h
  - src/lib/gui/MainWindow.cpp
  - src/lib/gui/widgets/StatusBar.h
  - src/lib/gui/widgets/StatusBar.cpp
  - src/lib/gui/core/CoreProcess.h
  - src/lib/gui/core/CoreProcess.cpp
  - src/lib/gui/dialogs/SettingsDialog.h
  - src/lib/gui/dialogs/SettingsDialog.cpp
  - src/lib/gui/dialogs/SettingsDialog.ui
  - src/lib/gui/Messages.cpp
  - src/lib/gui/Logger.cpp
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "Settings::Security struct does not exist -- no TlsEnabled, CheckPeers, Certificate, or KeySize keys"
    - "GUI MainWindow has no TLS certificate generation, fingerprint display, or security icon logic"
    - "StatusBar has no security icon, security level, or fingerprint button"
    - "CoreProcess has no secureSocket signal, peerFingerprint signal, or checkSecureSocket method"
    - "SettingsDialog has no TLS/encryption group box or controls"
    - "No TLS-related strings exist in Logger.cpp kForceDebugMessages"
  artifacts:
    - path: "src/lib/common/Settings.h"
      provides: "Settings without Security struct or TLS methods"
      not_contains: "Security"
      not_contains_2: "tlsDir"
    - path: "src/lib/gui/MainWindow.cpp"
      provides: "MainWindow without TLS code"
      not_contains: "TlsUtility"
    - path: "src/lib/gui/widgets/StatusBar.h"
      provides: "StatusBar without security methods"
      not_contains: "setSecurityIcon"
    - path: "src/lib/gui/core/CoreProcess.h"
      provides: "CoreProcess without TLS signals"
      not_contains: "secureSocket"
    - path: "src/lib/gui/dialogs/SettingsDialog.ui"
      provides: "Settings dialog without TLS group"
      not_contains: "groupSecurity"
  key_links:
    - from: "src/lib/gui/MainWindow.cpp"
      to: "CoreProcess"
      via: "signal connections"
      pattern: "secureSocket"
      must_not_exist: true
    - from: "src/lib/gui/dialogs/SettingsDialog.cpp"
      to: "Settings::Security"
      via: "settings read/write"
      pattern: "Settings::Security::"
      must_not_exist: true
---

<objective>
Strip all TLS/encryption references from Settings, GUI components, and IPC message handling.

Purpose: With TLS files deleted (Plan 02), the GUI code that referenced them will fail to compile. This plan removes all Security settings keys, TLS GUI logic from MainWindow, StatusBar security icons, CoreProcess IPC handlers for secureSocket/peerFingerprint, the SettingsDialog TLS controls, and Logger TLS filter entries. This completes the end-to-end removal.

Output: The GUI builds and runs with zero TLS references. All Settings::Security keys are removed. The StatusBar, MainWindow, CoreProcess, SettingsDialog, and Logger are clean of any encryption-related code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tls-encryption-removal/02-RESEARCH.md
@.planning/phases/02-tls-encryption-removal/02-01-SUMMARY.md
@.planning/phases/02-tls-encryption-removal/02-02-SUMMARY.md

<interfaces>
<!-- Settings::Security struct: to be removed entirely -->
From src/lib/common/Settings.h:
```cpp
struct Security {
  inline static const auto CheckPeers = QStringLiteral("security/checkPeerFingerprints");
  inline static const auto Certificate = QStringLiteral("security/certificate");
  inline static const auto KeySize = QStringLiteral("security/keySize");
  inline static const auto TlsEnabled = QStringLiteral("security/tlsEnabled");
};
```
Also in Settings.h: m_validKeys includes all 4 Security keys; m_defaultTrueValues includes TlsEnabled and CheckPeers
Also in Settings.h: static methods tlsDir(), tlsTrustedServersDb(), tlsTrustedClientsDb()

<!-- MainWindow: extensive TLS references -->
From src/lib/gui/MainWindow.h:
- Members: m_secureSocket, m_fingerprint, m_checkedClients, m_checkedServers
- Methods: updateSecurityIcon(), secureSocket(), handlePeerFingerprint(), showMyFingerprint(), updateLocalFingerprint(), trustedFingerprintDatabase(), generateCertificate()
From src/lib/gui/MainWindow.cpp:
- #include "dialogs/FingerprintDialog.h" (line 18)
- #include "gui/TlsUtility.h" (line 27)
- TLS cert generation in constructor (lines 142-158)
- Signal connections: secureSocket, securityLevelChanged, peerFingerprint (lines 248, 252, 275)
- Settings change handler for Security keys (lines 353-359)
- Multiple methods: updateSecurityIcon (580), secureSocket (1079), handlePeerFingerprint, trustedFingerprintDatabase (1166), generateCertificate (1171)

<!-- StatusBar: security display -->
From src/lib/gui/widgets/StatusBar.h:
- Methods: setSecurityIconVisible(), securityIconVisible(), updateSecurityInfo(), setSecurityIcon(), setSecurityLevel(), setBtnFingerprintVisible()
- Members: m_btnFingerprint, m_lblSecurityIcon, m_encrypted, m_securityLevel

<!-- CoreProcess: IPC for secureSocket and peerFingerprint -->
From src/lib/gui/core/CoreProcess.h:
- Signals: secureSocket(bool), securityLevelChanged(QString), peerFingerprint(QString)
- Members: m_secureSocketVersion
- Methods: secureSocketVersion(), checkSecureSocket()
From src/lib/gui/core/CoreProcess.cpp:
- IPC handler for "secureSocket" command (line 578-581)
- IPC handler for "peerFingerprint" command (line 598-599)
- checkSecureSocket() method (line 605-615) with tlsCheckString

<!-- SettingsDialog: TLS controls -->
From src/lib/gui/dialogs/SettingsDialog.h:
- Methods: updateTlsControls(), updateTlsControlsEnabled()
From src/lib/gui/dialogs/SettingsDialog.cpp:
- #include "gui/TlsUtility.h" (line 16)
- groupSecurity toggled connections (lines 105, 136)
- TLS cert generation (line 147)
- TLS settings save (lines 228-233)
- TLS settings load (lines 306-317)
- updateTlsControlsEnabled (line 325)
- TLS key length change handler (lines 407-409)
- TLS dirty check (lines 437-440)
- TLS defaults check (lines 472-475)
From src/lib/gui/dialogs/SettingsDialog.ui:
- groupSecurity QGroupBox (line 611-765+) containing: lineTlsCertPath, comboTlsKeyLength, btnTlsRegenCert, cbRequireClientCert, lblTlsCertInfo, lblTlsCert, widgetTlsCert, btnTlsCertPath, lblTlsKeyLength

<!-- Messages.cpp: TLS peer auth check -->
From src/lib/gui/Messages.cpp:
- showNewClientPrompt checks TlsEnabled + CheckPeers (lines 189-190)

<!-- Logger.cpp: TLS debug message filter -->
From src/lib/gui/Logger.cpp:
- kForceDebugMessages includes 3 TLS entries (lines 22-23):
  "No functional TLS backend was found"
  "No TLS backend is available"
  "QSslSocket::connectToHostEncrypted: TLS initialization failed"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TLS settings, GUI security controls, and IPC handlers</name>
  <files>
    src/lib/common/Settings.h,
    src/lib/common/Settings.cpp,
    src/lib/gui/MainWindow.h,
    src/lib/gui/MainWindow.cpp,
    src/lib/gui/widgets/StatusBar.h,
    src/lib/gui/widgets/StatusBar.cpp,
    src/lib/gui/core/CoreProcess.h,
    src/lib/gui/core/CoreProcess.cpp,
    src/lib/gui/dialogs/SettingsDialog.h,
    src/lib/gui/dialogs/SettingsDialog.cpp,
    src/lib/gui/dialogs/SettingsDialog.ui,
    src/lib/gui/Messages.cpp,
    src/lib/gui/Logger.cpp
  </files>
  <read_first>
    src/lib/common/Settings.h
    src/lib/common/Settings.cpp
    src/lib/gui/MainWindow.h
    src/lib/gui/MainWindow.cpp
    src/lib/gui/widgets/StatusBar.h
    src/lib/gui/widgets/StatusBar.cpp
    src/lib/gui/core/CoreProcess.h
    src/lib/gui/core/CoreProcess.cpp
    src/lib/gui/dialogs/SettingsDialog.h
    src/lib/gui/dialogs/SettingsDialog.cpp
    src/lib/gui/dialogs/SettingsDialog.ui
    src/lib/gui/Messages.cpp
    src/lib/gui/Logger.cpp
  </read_first>
  <behavior>
    - After changes, `grep -rn "Settings::Security" src/lib/common/ src/lib/gui/` returns zero matches
    - After changes, `grep -rn "TlsUtility\|FingerprintDialog\|FingerprintPreview" src/lib/gui/` returns zero matches
    - After changes, `grep -c "secureSocket\|peerFingerprint\|securityLevelChanged" src/lib/gui/core/CoreProcess.h` returns 0
    - After changes, `grep -c "setSecurityIcon\|setSecurityLevel\|setBtnFingerprintVisible\|m_encrypted\|m_securityLevel\|m_btnFingerprint\|m_lblSecurityIcon" src/lib/gui/widgets/StatusBar.h` returns 0
    - After changes, `grep -c "groupSecurity" src/lib/gui/dialogs/SettingsDialog.ui` returns 0
    - After changes, `grep -c "TLS" src/lib/gui/Logger.cpp` returns 0
  </behavior>
  <action>
    Read each file fully before modifying. Perform the following changes:

    **A. src/lib/common/Settings.h:**
    - Remove the entire `struct Security` block (lines 97-103):
      ```cpp
      struct Security {
        inline static const auto CheckPeers = ...;
        inline static const auto Certificate = ...;
        inline static const auto KeySize = ...;
        inline static const auto TlsEnabled = ...;
      };
      ```
    - Remove from `m_validKeys` list:
      - `Settings::Security::Certificate`
      - `Settings::Security::CheckPeers`
      - `Settings::Security::KeySize`
      - `Settings::Security::TlsEnabled`
    - Remove from `m_defaultTrueValues` list:
      - `Settings::Security::TlsEnabled`
      - `Settings::Security::CheckPeers`
    - Remove these static method declarations:
      - `static QString tlsDir();`
      - `static QString tlsTrustedServersDb();`
      - `static QString tlsTrustedClientsDb();`

    **B. src/lib/common/Settings.cpp:**
    - Remove the implementations of `tlsDir()`, `tlsTrustedServersDb()`, `tlsTrustedClientsDb()`
    - In `defaultValue()`: remove the cases for `Security::Certificate` and `Security::KeySize` (lines 174-177)

    **C. src/lib/gui/MainWindow.h:**
    - Remove `#include "net/Fingerprint.h"` if present
    - Remove member declarations:
      - `bool m_secureSocket = false;`
      - Any fingerprint-related member (m_fingerprint, m_checkedClients, m_checkedServers)
    - Remove method declarations:
      - `void updateSecurityIcon(bool visible);`
      - `void secureSocket(bool secureSocket);`
      - `void handlePeerFingerprint(const QString &fingerprint);` (check actual signature)
      - `void showMyFingerprint();` if present
      - `void updateLocalFingerprint();` if present
      - `QString trustedFingerprintDatabase(bool isClient);` if present
      - `void generateCertificate();` if present

    **D. src/lib/gui/MainWindow.cpp:**
    - Remove `#include "dialogs/FingerprintDialog.h"` (line 18)
    - Remove `#include "gui/TlsUtility.h"` (line 27)
    - In constructor: remove the TLS certificate generation block (lines 142-158) -- the entire `if (TlsUtility::isEnabled())` block including key size check, cert validity check, and fingerprint generation. Also remove `m_statusBar->setSecurityIcon(TlsUtility::isEnabled());` (line 158).
    - In constructor: remove `secureSocket(false);` call (line 195)
    - In signal connection setup: remove these 3 connects (lines 248, 252, 275):
      - `connect(&m_coreProcess, &CoreProcess::secureSocket, this, &MainWindow::secureSocket);`
      - `connect(&m_coreProcess, &CoreProcess::securityLevelChanged, m_statusBar, &StatusBar::setSecurityLevel);`
      - `connect(&m_coreProcess, &CoreProcess::peerFingerprint, this, &MainWindow::handlePeerFingerprint);`
    - In settingsChanged handler: remove the Security key check block (lines 353-359). Replace with an empty block or remove the condition for the 4 Security keys.
    - Remove the entire `updateSecurityIcon()` method implementation
    - Remove the entire `secureSocket()` method implementation
    - Remove the entire `handlePeerFingerprint()` method implementation
    - Remove the entire `trustedFingerprintDatabase()` method implementation
    - Remove the entire `generateCertificate()` method implementation (or `showMyFingerprint()` if it exists)
    - In any method that calls `m_statusBar->setBtnFingerprintVisible(...)`: remove the call
    - In any method that calls `FingerprintDialog`: remove the dialog creation code
    - Remove any calls to `TlsUtility::` functions

    **E. src/lib/gui/widgets/StatusBar.h:**
    - Remove method declarations:
      - `void setSecurityIconVisible(bool visible);`
      - `bool securityIconVisible() const;`
      - `void updateSecurityInfo(bool encrypted);`
      - `void setSecurityIcon(bool encrypted);`
      - `void setSecurityLevel(const QString &securityLevel);`
      - `void setBtnFingerprintVisible(bool visible);`
    - Remove member declarations:
      - `QPushButton *m_btnFingerprint = nullptr;`
      - `QLabel *m_lblSecurityIcon = nullptr;`
      - `bool m_encrypted = false;`
      - `QString m_securityLevel;`
    - Remove the `requestShowMyFingerprints` signal if it exists

    **F. src/lib/gui/widgets/StatusBar.cpp:**
    - Remove implementations of all methods removed from StatusBar.h
    - Remove creation and layout of m_btnFingerprint and m_lblSecurityIcon in constructor
    - Remove any reference to security icon, fingerprint button, encryption state

    **G. src/lib/gui/core/CoreProcess.h:**
    - Remove signal declarations:
      - `void secureSocket(bool enabled);`
      - `void securityLevelChanged(QString securityLevel);`
      - `void peerFingerprint(const QString &fingerprint);`
    - Remove method declarations:
      - `QString secureSocketVersion() const` (getter) and `m_secureSocketVersion` member
      - `bool checkSecureSocket(const QString &line);` (private method)

    **H. src/lib/gui/core/CoreProcess.cpp:**
    - In `onCoreIpcMessageReceived()` or the IPC handler method: remove the `else if (command == "secureSocket")` block and the `else if (command == "peerFingerprint")` block
    - Remove the entire `checkSecureSocket()` method implementation
    - Remove `static const QString tlsCheckString = "network encryption protocol: ";` and any related logic in `onProcessReadyReadStandardOutput()` or `handleLogLines()` that calls checkSecureSocket

    **I. src/lib/gui/dialogs/SettingsDialog.h:**
    - Remove method declarations: `updateTlsControls()`, `updateTlsControlsEnabled()`

    **J. src/lib/gui/dialogs/SettingsDialog.cpp:**
    - Remove `#include "gui/TlsUtility.h"` (line 16)
    - Remove all signal connections involving `ui->groupSecurity` (lines 105, 136)
    - Remove TLS certificate generation call (line 147)
    - Remove TLS settings save (lines 228-233): `Settings::setValue(Settings::Security::Certificate, ...)`, `Settings::Security::KeySize`, `Settings::Security::TlsEnabled`, `Settings::Security::CheckPeers`
    - Remove TLS settings load (lines 306-317): certificate path, combo key length, cbRequireClientCert, groupSecurity setChecked/setEnabled
    - Remove `updateTlsControlsEnabled()` method: replace TLS-specific logic or remove entirely
    - Remove TLS key length change handler (lines 407-409)
    - Remove TLS entries from dirty check (lines 437-440)
    - Remove TLS entries from defaults check (lines 472-475)
    - Remove all references to `ui->lineTlsCertPath`, `ui->comboTlsKeyLength`, `ui->cbRequireClientCert`, `ui->groupSecurity`, `ui->btnTlsRegenCert`, `ui->btnTlsCertPath`

    **K. src/lib/gui/dialogs/SettingsDialog.ui:**
    - Remove the entire `<item>` block containing the `<widget class="QGroupBox" name="groupSecurity">` and all its child widgets. This is the `<item>` starting at line 610 through approximately line 770 (the closing `</layout></widget></item>`). Be careful to remove only the groupSecurity item, not surrounding layout items.
    - Remove any other TLS-related widget declarations if found outside groupSecurity

    **L. src/lib/gui/Messages.cpp:**
    - In `showNewClientPrompt()`: remove the TLS peer auth check (lines 189-190). The function should always show the dialog with the "ignore" option. Simplify to always show the QMessageBox with the "ignore new client" option regardless of TLS settings.

    **M. src/lib/gui/Logger.cpp:**
    - Remove these 3 entries from `kForceDebugMessages` (lines 22-23):
      - `QStringLiteral("No functional TLS backend was found")`
      - `QStringLiteral("No TLS backend is available")`
      - `QStringLiteral("QSslSocket::connectToHostEncrypted: TLS initialization failed")`
    - Keep the non-TLS entries: "Retrying to obtain clipboard." and "Unable to obtain clipboard."
  </action>
  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && grep -rn "Settings::Security\|TlsUtility\|FingerprintDialog\|FingerprintPreview\|secureSocket\|peerFingerprint\|groupSecurity\|setSecurityIcon\|setSecurityLevel" src/lib/common/Settings.h src/lib/common/Settings.cpp src/lib/gui/MainWindow.h src/lib/gui/MainWindow.cpp src/lib/gui/widgets/StatusBar.h src/lib/gui/widgets/StatusBar.cpp src/lib/gui/core/CoreProcess.h src/lib/gui/core/CoreProcess.cpp src/lib/gui/dialogs/SettingsDialog.h src/lib/gui/dialogs/SettingsDialog.cpp src/lib/gui/dialogs/SettingsDialog.ui src/lib/gui/Messages.cpp src/lib/gui/Logger.cpp 2>/dev/null; echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct Security" src/lib/common/Settings.h` returns 0
    - `grep -c "Settings::Security::" src/lib/common/Settings.h src/lib/common/Settings.cpp` returns 0 for each
    - `grep -c "tlsDir\|tlsTrustedServersDb\|tlsTrustedClientsDb" src/lib/common/Settings.h src/lib/common/Settings.cpp` returns 0 for each
    - `grep -c "TlsUtility\|FingerprintDialog\|FingerprintPreview" src/lib/gui/MainWindow.cpp` returns 0
    - `grep -c "m_secureSocket\|m_fingerprint\|m_checkedClients\|m_checkedServers" src/lib/gui/MainWindow.h` returns 0
    - `grep -c "setSecurityIcon\|setSecurityLevel\|setBtnFingerprintVisible\|m_btnFingerprint\|m_lblSecurityIcon\|m_encrypted\|m_securityLevel" src/lib/gui/widgets/StatusBar.h` returns 0
    - `grep -c "secureSocket\|peerFingerprint\|securityLevelChanged\|m_secureSocketVersion\|checkSecureSocket\|tlsCheckString" src/lib/gui/core/CoreProcess.h src/lib/gui/core/CoreProcess.cpp` returns 0 for each
    - `grep -c "groupSecurity\|lineTlsCertPath\|comboTlsKeyLength\|cbRequireClientCert\|TlsUtility\|updateTlsControls" src/lib/gui/dialogs/SettingsDialog.cpp src/lib/gui/dialogs/SettingsDialog.ui` returns 0 for each
    - `grep -c "groupSecurity" src/lib/gui/dialogs/SettingsDialog.ui` returns 0
    - `grep -c "TLS" src/lib/gui/Logger.cpp` returns 0
    - `grep -c "TlsEnabled\|CheckPeers" src/lib/gui/Messages.cpp` returns 0
    - `cmake --build build --target gui` succeeds with zero errors
    - `cmake --build build --target common` succeeds with zero errors
  </acceptance_criteria>
  <done>
    - Settings.h has no Security struct, no tlsDir/tlsTrustedServersDb/tlsTrustedClientsDb, no Security keys in validKeys/defaultTrueValues
    - Settings.cpp has no TLS method implementations or TLS default values
    - MainWindow has no TLS includes, no TLS cert generation, no fingerprint dialog, no security icon, no TLS signal connections
    - StatusBar has no security icon, no security level, no fingerprint button
    - CoreProcess has no secureSocket/peerFingerprint/securityLevelChanged signals, no checkSecureSocket method
    - SettingsDialog has no TLS group box, no TLS controls, no TLS settings save/load
    - SettingsDialog.ui has no groupSecurity widget
    - Messages.cpp does not check TLS settings for peer auth
    - Logger.cpp has no TLS debug message filter entries
    - All affected targets compile successfully
  </done>
</task>

</tasks>

<verification>
1. Grep Settings::Security across common and gui: zero matches
2. Grep TlsUtility/FingerprintDialog across gui: zero matches
3. Grep secureSocket/peerFingerprint in CoreProcess: zero matches
4. Grep setSecurityIcon/setSecurityLevel in StatusBar: zero matches
5. Grep groupSecurity in SettingsDialog.ui: zero matches
6. Grep TLS in Logger.cpp: zero matches
7. Build gui target: succeeds
8. Build common target: succeeds
</verification>

<success_criteria>
- Settings::Security struct and all 4 keys removed from code
- MainWindow has zero TLS references (no cert generation, no fingerprint, no security icon)
- StatusBar has zero security display code
- CoreProcess has no secureSocket or peerFingerprint IPC handling
- SettingsDialog has no TLS controls in code or UI
- Messages.cpp shows new client prompt without TLS check
- Logger.cpp has no TLS debug filter entries
- gui and common library targets compile successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-tls-encryption-removal/02-03-SUMMARY.md`
</output>

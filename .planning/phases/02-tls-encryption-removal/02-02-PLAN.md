---
phase: 02-tls-encryption-removal
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/net/SecurityLevel.h
  - src/lib/net/SecureSocket.h
  - src/lib/net/SecureSocket.cpp
  - src/lib/net/SecureListenSocket.h
  - src/lib/net/SecureListenSocket.cpp
  - src/lib/net/SecureUtils.h
  - src/lib/net/SecureUtils.cpp
  - src/lib/net/SslLogger.h
  - src/lib/net/SslLogger.cpp
  - src/lib/net/Fingerprint.h
  - src/lib/net/Fingerprint.cpp
  - src/lib/net/FingerprintDatabase.h
  - src/lib/net/FingerprintDatabase.cpp
  - src/lib/net/CMakeLists.txt
  - src/lib/gui/TlsUtility.h
  - src/lib/gui/TlsUtility.cpp
  - src/lib/gui/dialogs/FingerprintDialog.h
  - src/lib/gui/dialogs/FingerprintDialog.cpp
  - src/lib/gui/widgets/FingerprintPreview.h
  - src/lib/gui/widgets/FingerprintPreview.cpp
  - src/lib/gui/CMakeLists.txt
  - src/unittests/net/SecureUtilsTests.h
  - src/unittests/net/SecureUtilsTests.cpp
  - src/unittests/net/FingerprintTests.h
  - src/unittests/net/FingerprintTests.cpp
  - src/unittests/net/FingerprintDatabaseTests.h
  - src/unittests/net/FingerprintDatabaseTests.cpp
  - src/unittests/net/CMakeLists.txt
  - src/unittests/common/SettingsTests.h
  - src/unittests/common/SettingsTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "No TLS/SSL implementation file exists in src/lib/net/"
    - "No TLS GUI utility file exists in src/lib/gui/"
    - "No TLS test file exists in src/unittests/net/"
    - "net library CMakeLists.txt has no OpenSSL find_package or link deps"
    - "Root CMakeLists.txt has no REQUIRED_OPENSSL_VERSION variable"
    - "No SecureSocket, SecureListenSocket, SecureUtils, SslLogger, Fingerprint, or FingerprintDatabase source files exist"
  artifacts:
    - path: "src/lib/net/CMakeLists.txt"
      provides: "net library build without OpenSSL dependency"
      contains: "find_package(Qt6"
      not_contains: "OpenSSL"
    - path: "CMakeLists.txt"
      provides: "Root build without REQUIRED_OPENSSL_VERSION"
      not_contains: "REQUIRED_OPENSSL_VERSION"
  key_links:
    - from: "src/lib/net/CMakeLists.txt"
      to: "OpenSSL"
      via: "build dependency"
      pattern: "OpenSSL"
      must_not_exist: true
---

<objective>
Delete all TLS/SSL implementation files, test files, and remove OpenSSL build dependencies.

Purpose: With SecurityLevel removed from all interfaces (Plan 01), the TLS implementation files are dead code. This plan physically removes all SecureSocket, SecureUtils, SslLogger, Fingerprint, FingerprintDatabase files from net/, all TLS GUI files (TlsUtility, FingerprintDialog, FingerprintPreview) from gui/, all TLS test files from unittests/, and strips OpenSSL from CMake build dependencies.

Output: Zero TLS implementation or test files remain. Build no longer depends on OpenSSL. The net library compiles with only plain-text socket code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tls-encryption-removal/02-RESEARCH.md
@.planning/phases/02-tls-encryption-removal/02-01-SUMMARY.md

<interfaces>
<!-- Net CMakeLists: current state includes OpenSSL and TLS files -->
From src/lib/net/CMakeLists.txt:
```cmake
if(APPLE)
  set(OPENSSL_USE_STATIC_LIBS TRUE)
endif()
find_package(Qt6 ${REQUIRED_QT_VERSION} REQUIRED COMPONENTS Network)
find_package(OpenSSL ${REQUIRED_OPENSSL_VERSION} REQUIRED COMPONENTS SSL Crypto)

add_library(net STATIC
  # ... TLS files to remove:
  Fingerprint.cpp/h
  FingerprintDatabase.cpp/h
  SecureListenSocket.cpp/h
  SecurityLevel.h
  SecureSocket.cpp/h
  SecureUtils.cpp/h
  SslLogger.cpp/h
)

target_link_libraries(net
  PUBLIC OpenSSL::SSL OpenSSL::Crypto Qt6::Network common asio
  PRIVATE mt io)

if(WIN32)
  target_link_libraries(net PUBLIC OpenSSL::applink PRIVATE Crypt32 ws2_32)
endif()
```

<!-- GUI CMakeLists: TLS files to remove -->
Files to remove from src/lib/gui/CMakeLists.txt:
- dialogs/FingerprintDialog.cpp/h
- widgets/FingerprintPreview.cpp/h

<!-- Test CMakeLists: TLS test targets to remove -->
From src/unittests/net/CMakeLists.txt:
```cmake
create_test(NAME SecureUtilsTests ...)
create_test(NAME FingerprintTests ...)
create_test(NAME FingerprintDatabaseTests ...)
```

<!-- Root CMakeLists: OpenSSL version variable -->
From CMakeLists.txt line 100:
```cmake
set(REQUIRED_OPENSSL_VERSION 3.0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete TLS implementation files and remove OpenSSL from build</name>
  <files>
    src/lib/net/SecurityLevel.h,
    src/lib/net/SecureSocket.h,
    src/lib/net/SecureSocket.cpp,
    src/lib/net/SecureListenSocket.h,
    src/lib/net/SecureListenSocket.cpp,
    src/lib/net/SecureUtils.h,
    src/lib/net/SecureUtils.cpp,
    src/lib/net/SslLogger.h,
    src/lib/net/SslLogger.cpp,
    src/lib/net/Fingerprint.h,
    src/lib/net/Fingerprint.cpp,
    src/lib/net/FingerprintDatabase.h,
    src/lib/net/FingerprintDatabase.cpp,
    src/lib/net/CMakeLists.txt,
    src/lib/gui/TlsUtility.h,
    src/lib/gui/TlsUtility.cpp,
    src/lib/gui/dialogs/FingerprintDialog.h,
    src/lib/gui/dialogs/FingerprintDialog.cpp,
    src/lib/gui/widgets/FingerprintPreview.h,
    src/lib/gui/widgets/FingerprintPreview.cpp,
    src/lib/gui/CMakeLists.txt,
    src/unittests/net/SecureUtilsTests.h,
    src/unittests/net/SecureUtilsTests.cpp,
    src/unittests/net/FingerprintTests.h,
    src/unittests/net/FingerprintTests.cpp,
    src/unittests/net/FingerprintDatabaseTests.h,
    src/unittests/net/FingerprintDatabaseTests.cpp,
    src/unittests/net/CMakeLists.txt,
    src/unittests/common/SettingsTests.h,
    src/unittests/common/SettingsTests.cpp,
    CMakeLists.txt
  </files>
  <read_first>
    src/lib/net/CMakeLists.txt
    src/lib/gui/CMakeLists.txt
    src/unittests/net/CMakeLists.txt
    src/unittests/common/SettingsTests.h
    src/unittests/common/SettingsTests.cpp
    CMakeLists.txt
  </read_first>
  <behavior>
    - After changes, `ls src/lib/net/SecureSocket.h src/lib/net/SecureSocket.cpp src/lib/net/SecureListenSocket.h src/lib/net/SecureListenSocket.cpp src/lib/net/SecureUtils.h src/lib/net/SecureUtils.cpp src/lib/net/SslLogger.h src/lib/net/SslLogger.cpp src/lib/net/Fingerprint.h src/lib/net/Fingerprint.cpp src/lib/net/FingerprintDatabase.h src/lib/net/FingerprintDatabase.cpp src/lib/net/SecurityLevel.h 2>&1` shows "No such file or directory" for every file
    - After changes, `ls src/lib/gui/TlsUtility.h src/lib/gui/TlsUtility.cpp src/lib/gui/dialogs/FingerprintDialog.h src/lib/gui/dialogs/FingerprintDialog.cpp src/lib/gui/widgets/FingerprintPreview.h src/lib/gui/widgets/FingerprintPreview.cpp 2>&1` shows "No such file or directory" for every file
    - After changes, `ls src/unittests/net/SecureUtilsTests.h src/unittests/net/SecureUtilsTests.cpp src/unittests/net/FingerprintTests.h src/unittests/net/FingerprintTests.cpp src/unittests/net/FingerprintDatabaseTests.h src/unittests/net/FingerprintDatabaseTests.cpp 2>&1` shows "No such file or directory" for every file
    - After changes, `grep -c "OpenSSL" src/lib/net/CMakeLists.txt` returns 0
    - After changes, `grep -c "REQUIRED_OPENSSL_VERSION" CMakeLists.txt` returns 0
    - SettingsTests.h and SettingsTests.cpp have no tlsDir, tlsTrustedServersDb, tlsTrustedClientsDb test methods
  </behavior>
  <action>
    Perform all file deletions and CMake modifications:

    **A. Delete net library TLS files (13 files):**
    ```
    rm src/lib/net/SecurityLevel.h
    rm src/lib/net/SecureSocket.h src/lib/net/SecureSocket.cpp
    rm src/lib/net/SecureListenSocket.h src/lib/net/SecureListenSocket.cpp
    rm src/lib/net/SecureUtils.h src/lib/net/SecureUtils.cpp
    rm src/lib/net/SslLogger.h src/lib/net/SslLogger.cpp
    rm src/lib/net/Fingerprint.h src/lib/net/Fingerprint.cpp
    rm src/lib/net/FingerprintDatabase.h src/lib/net/FingerprintDatabase.cpp
    ```

    **B. Delete GUI TLS files (6 files):**
    ```
    rm src/lib/gui/TlsUtility.h src/lib/gui/TlsUtility.cpp
    rm src/lib/gui/dialogs/FingerprintDialog.h src/lib/gui/dialogs/FingerprintDialog.cpp
    rm src/lib/gui/widgets/FingerprintPreview.h src/lib/gui/widgets/FingerprintPreview.cpp
    ```

    **C. Delete TLS test files (6 files):**
    ```
    rm src/unittests/net/SecureUtilsTests.h src/unittests/net/SecureUtilsTests.cpp
    rm src/unittests/net/FingerprintTests.h src/unittests/net/FingerprintTests.cpp
    rm src/unittests/net/FingerprintDatabaseTests.h src/unittests/net/FingerprintDatabaseTests.cpp
    ```

    **D. Modify src/lib/net/CMakeLists.txt:**
    - Remove the entire Apple OpenSSL static lib block (lines 7-9):
      ```
      if(APPLE)
        set(OPENSSL_USE_STATIC_LIBS TRUE)
      endif()
      ```
    - Remove the OpenSSL find_package line (line 14):
      ```
      find_package(OpenSSL ${REQUIRED_OPENSSL_VERSION} REQUIRED COMPONENTS SSL Crypto)
      ```
    - Remove these entries from the add_library source list:
      - `Fingerprint.cpp` / `Fingerprint.h`
      - `FingerprintDatabase.cpp` / `FingerprintDatabase.h`
      - `SecureListenSocket.cpp` / `SecureListenSocket.h`
      - `SecurityLevel.h`
      - `SecureSocket.cpp` / `SecureSocket.h`
      - `SecureUtils.cpp` / `SecureUtils.h`
      - `SslLogger.cpp` / `SslLogger.h`
    - In target_link_libraries: remove `OpenSSL::SSL OpenSSL::Crypto` from the PUBLIC section. The remaining PUBLIC deps should be `Qt6::Network common asio`
    - Remove the entire `if(WIN32)` block that adds `OpenSSL::applink`:
      ```
      if(WIN32)
        target_link_libraries(net PUBLIC OpenSSL::applink PRIVATE Crypt32 ws2_32)
      endif()
      ```
      Note: If ws2_32 and Crypt32 are still needed for non-TLS reasons, keep them in a simplified WIN32 block without OpenSSL::applink.

    **E. Modify src/lib/gui/CMakeLists.txt:**
    - Remove from source list:
      - `dialogs/FingerprintDialog.cpp` / `dialogs/FingerprintDialog.h`
      - `widgets/FingerprintPreview.cpp` / `widgets/FingerprintPreview.h`

    **F. Replace src/unittests/net/CMakeLists.txt:**
    - Remove the three create_test blocks for SecureUtilsTests, FingerprintTests, and FingerprintDatabaseTests. Keep only any non-TLS test targets that may exist. If the file becomes empty (no remaining tests), leave the CMakeLists.txt with just the license header and a comment.

    **G. Modify src/unittests/common/SettingsTests.h:**
    - Remove these test method declarations:
      - `void tlsDir();`
      - `void tlsTrustedServersDb();`
      - `void tlsTrustedClientsDb();`

    **H. Modify src/unittests/common/SettingsTests.cpp:**
    - Remove the test method implementations for tlsDir(), tlsTrustedServersDb(), tlsTrustedClientsDb()
    - Remove the member variables `m_expectedTlsDir`, `m_expectedTlsServerDB`, `m_expectedTlsClientDB` if they are only used by those tests

    **I. Modify CMakeLists.txt (root):**
    - Remove the line `set(REQUIRED_OPENSSL_VERSION 3.0)` (line 100)
  </action>
  <verify>
    <automated>cd /Users/yzs/work/project/code/deskflow && grep -c "OpenSSL" src/lib/net/CMakeLists.txt && grep -c "REQUIRED_OPENSSL_VERSION" CMakeLists.txt && ls src/lib/net/SecureSocket.h src/lib/net/SecurityLevel.h src/lib/gui/TlsUtility.h src/unittests/net/SecureUtilsTests.cpp 2>&1 | grep -c "No such file"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/lib/net/SecureSocket.h 2>&1` contains "No such file or directory"
    - `ls src/lib/net/SecurityLevel.h 2>&1` contains "No such file or directory"
    - `ls src/lib/net/SslLogger.h 2>&1` contains "No such file or directory"
    - `ls src/lib/net/FingerprintDatabase.h 2>&1` contains "No such file or directory"
    - `ls src/lib/gui/TlsUtility.h 2>&1` contains "No such file or directory"
    - `ls src/lib/gui/dialogs/FingerprintDialog.h 2>&1` contains "No such file or directory"
    - `ls src/lib/gui/widgets/FingerprintPreview.h 2>&1` contains "No such file or directory"
    - `ls src/unittests/net/SecureUtilsTests.cpp 2>&1` contains "No such file or directory"
    - `grep -c "OpenSSL" src/lib/net/CMakeLists.txt` returns 0
    - `grep -c "REQUIRED_OPENSSL_VERSION" CMakeLists.txt` returns 0
    - `grep -c "FingerprintDialog\|FingerprintPreview" src/lib/gui/CMakeLists.txt` returns 0
    - `grep -c "tlsDir\|tlsTrustedServersDb\|tlsTrustedClientsDb" src/unittests/common/SettingsTests.h` returns 0
    - `cmake --build build --target net` succeeds with zero errors
  </acceptance_criteria>
  <done>
    - All 13 net TLS files deleted (SecurityLevel.h, SecureSocket.h/cpp, SecureListenSocket.h/cpp, SecureUtils.h/cpp, SslLogger.h/cpp, Fingerprint.h/cpp, FingerprintDatabase.h/cpp)
    - All 6 GUI TLS files deleted (TlsUtility.h/cpp, FingerprintDialog.h/cpp, FingerprintPreview.h/cpp)
    - All 6 TLS test files deleted (SecureUtilsTests.h/cpp, FingerprintTests.h/cpp, FingerprintDatabaseTests.h/cpp)
    - net/CMakeLists.txt has no OpenSSL find_package, no OpenSSL link deps, no TLS source files
    - gui/CMakeLists.txt has no TLS source files
    - unittests/net/CMakeLists.txt has no TLS test targets
    - SettingsTests has no TLS-related test methods
    - Root CMakeLists.txt has no REQUIRED_OPENSSL_VERSION
    - net library target compiles successfully
  </done>
</task>

</tasks>

<verification>
1. ls of all deleted files: all return "No such file or directory"
2. grep OpenSSL in net/CMakeLists.txt: zero matches
3. grep REQUIRED_OPENSSL_VERSION in root CMakeLists.txt: zero matches
4. cmake --build build --target net: succeeds
5. cmake --build build --target gui: succeeds (after Plan 03 completes GUI cleanup)
</verification>

<success_criteria>
- Zero TLS implementation files exist in src/lib/net/
- Zero TLS GUI files exist in src/lib/gui/
- Zero TLS test files exist in src/unittests/net/
- net library builds without OpenSSL dependency
- Root CMakeLists.txt has no OpenSSL version requirement
</success_criteria>

<output>
After completion, create `.planning/phases/02-tls-encryption-removal/02-02-SUMMARY.md`
</output>
